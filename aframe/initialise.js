// Aframe initialisation imports. Must be imported in the applications index.js

import './lib/THREE.AudioLoader'; // Replaces the THREE.AudioLoader object
import './lib/transform'; // replaces the window.Transform object
